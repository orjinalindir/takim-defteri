import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary - Gold
  static const Color primaryGold = Color(0xFFD4A017);
  static const Color primaryGoldLight = Color(0xFFF5E6A3);

  // Secondary - Navy
  static const Color secondaryNavy = Color(0xFF1A2744);
  static const Color secondaryNavyLight = Color(0xFF2E3F6B);

  // Backgrounds
  static const Color backgroundLight = Color(0xFFFAF8F2);
  static const Color backgroundDark = Color(0xFF0F1621);

  // Surface
  static const Color surfaceGold = Color(0xFFFFF8E1);

  // Error
  static const Color errorRed = Color(0xFFB00020);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF666666);

  // Misc
  static const Color white = Colors.white;
  static const Color divider = Color(0xFFE0E0E0);
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA000);
}
